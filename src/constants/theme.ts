// Single source of truth for the brand accent color, for contexts that can't
// consume the `--color-brand` CSS variable (three.js/react-three-fiber color
// props, canvas gradients, etc). Keep in sync with `--color-brand` in index.css.
export const BRAND_HEX = '#FE6D00';
